spices = [
    'salt',
    'pepper',
    'cumin',
    'turmeric',
]

#(for) specifies a variable name that we can use in each iteration of the loop.
#(in) indicates that what follows is the set of values that we want to iterate.
for spice in spices:
    print(spice)
print('No more boring omelettes!')
